<html id="ht">
<head>
	<meta http-equiv="content-type" content="text/html;charset=utf-8">
	<title>一个html文档</title>
	<script language="javascript">
		//遍历的第一种形式:
		var elementList=""//用于保存
		function getElement(node){
			var total=0;
			if(node.nodeType==1){
				alert(node.name);
				total++;
				elementList=elementList+node.nodeName+",";
			}
			var childrens=node.childrens;//获得node的全部子节点
			for(var m=node.firstChild;m!=null;m=m.nextSibling){
					total+=getElement(m);//对每个节点进行递归调用;
			}
			return total;
		}
		function show(){
			var number=getElement(document);//获得标记总数;
			elementlist=elementList.substring(0,elementList.length-1);
			alert("该文档中包含:"+elementList+"等"+number+"个标记");
			elemenList="";//清空全局变量;
		}
		//第二种方式:递归调用;
		function getElements(node){
			if(node.nodeType==1){//检查node是否为Element对象
				alert(node.nodeName);
				 var childrens=node.childNodes;
				 for(var m=node.firstChild;m!=null;m=m.nextSibling){
					getElement(m);
				}
			}
		}
	</script>
</head>

<body onload="getElements(document.getElementById('ht'))">
	大家好,
	<br>
	//Dom文档对象模型,表示文档和访问操作文档的应用接口;
	<br>
	//直接可以把一个网站映射到a标签上;
	<a href="http://www.java1995.com"/>java资源库</a><br/>
</body>
</html>